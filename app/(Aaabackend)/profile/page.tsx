import React from "react";

const page = () => {
  return (
    <div>
      If api folder wasn't created then route.ts is superior than page.tsx
      meaning content of route.ts will appear not the frontend
    </div>
  );
};

export default page;
